<%= form_with(model: vote, local: true, id: "_f") do |form| %>
  <% if vote.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(vote.errors.count, "error") %> prohibited this vote from being saved:</h2>

      <ul>
      <% vote.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <!--<div class="field">
    <%#= form.label :cpf %>
    <%#= form.text_field :cpf %>
  </div>-->

  <div class="form-group">
    <%#= form.label('Voto') %>
    <%= form.number_field :vote, class: "form-control", style: "display:none", id: "_voto" %>
  </div>

  <%#= form.submit('Enviar Voto') %>
<% end %>
